function(context, args) {
	//var AsciiBanner = require('ascii-banner');
var banner = #s.kode.headline({text:"skuliy.hub", return_output:true}).msg;
	// simple banner
	//return AsciiBanner.write('My Text').out();
	return banner;
}
