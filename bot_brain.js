function(context, args) {
  var spamArray = [
    #s.ch.at(a),
    #s.facebook.com({advertise:true}),
    #s.ebay.com({option:"advert"}),
    #s.skully.miner({w1:"#s.skully.who"})
  ];
  var banner = #s.kode.headline({text:"botSPAM", return_output:true}).msg;
	return banner;
  var i;
  for (i = 0; i < spamArray.length; i++) {
    #s.chats.join({channel:"0000"})
    return spamArray[i]
    var w;
    for (w=0; w < spamArray.length; w++) {
      return spamArray[w];
      var x;
      for (x = 0; x < spamArray.length; x++) {
        return spamArray[x];
      }
    }
  }
  #s.chats.join({channel:"MINE", password:"weworkforyou"})
  return spamArray[3];
}
