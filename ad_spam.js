function (c, a) {
  var spamRand = Math.floor(Math.random() * 11);
  var spamArray = [
    //#s.wfl.loc_list({action:"advertise",channel:"0000"})
    //#s.ebay.com({option:"advert"})
    #s.ch.at(a),
    #s.ch.at(a),
    #s.ch.at(a),
    #s.ch.at(a),
    #s.ch.at(a),
    #s.ch.at(a),
    #s.ch.at(a),
    #s.ch.at(a),
    #s.ch.at(a),
    #s.ch.at(a),
    #s.facebook.com({advertise:true})
  ];
  #s.chats.join({channel:"0000"})
  var i;
  for (i = 0; i < spamArray.length; i++) {
    return spamArray[spamRand];
  }
  var v;
  for (v = 0; v < spamArray.length; v++) {
    return spamArray[spamRand]
    var w;
    for (w=0; w < spamArray.length; w++) {
      return spamArray[spamRand];
      var x;
      for (x = 0; x < spamArray.length; x++) {
        return spamArray[spamRand];
      }
      return {
        ok:true
      }
    }
  }

  //if (#s.scripts.get_level({name:"ch.at"}) == 4) {
  //  return array[rand];
  //}
}
